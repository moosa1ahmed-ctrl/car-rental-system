<Window x:Class="CarRental_CP317.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarRental_CP317"
        mc:Ignorable="d"
        Title="Car Rental" Height="714" Width="1270"
        Background="#FF1A1A1A"
        SizeToContent="Manual"
        MinHeight="714" MinWidth="1270">
    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value="0.5, 0.5"/>
            <Setter Property="Background" Value="#FFE05953"/>
            <Setter Property="BorderBrush" Value="#FFB34944"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="0"
                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransformOrigin" Value="0.5, 0.5"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95" CenterX="50" CenterY="20"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NoButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>

                        <!-- Use a Border so Background/BorderBrush/BorderThickness work if set on the Button -->
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Margin="{TemplateBinding Padding}"
                            RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Viewbox Stretch="UniformToFill" StretchDirection="Both" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid MinWidth="1894" MinHeight="1050">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top bar that stretches to full window width (within the design grid) -->
            <Grid x:Name="TopBar" Panel.ZIndex="100" Grid.Row="0" Height="60" Background="#0F0F0F" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <!-- Left navigation -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Spacer absorbs remaining width -->
                    <ColumnDefinition Width="*"/>
                    <!-- Right account area -->
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left navigation: UniformGrid distributes buttons evenly within its allocated space -->
                <UniformGrid Grid.Column="0" Panel.ZIndex="101" Rows="1" Columns="4" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.ColumnSpan="2" Width="816" Margin="0,15,0,0">
                    <Button x:Name="TopBarDashboardButton" Content="Dashboard" Style="{StaticResource NoButtonStyle}" Height="26" FontWeight="SemiBold" FontSize="18" Foreground="White" Background="{x:Null}" BorderBrush="{x:Null}" />
                    <Button x:Name="TopBarRentalsButton" Content="Browse Rentals" Style="{StaticResource NoButtonStyle}" Height="26" FontWeight="SemiBold" FontSize="18" Foreground="White" Background="{x:Null}" BorderBrush="{x:Null}" Click="TopBarRentalsButton_Click" />
                    <Button x:Name="TopBarBookingsButton" Content="My Bookings" Style="{StaticResource NoButtonStyle}" Height="26" FontWeight="SemiBold" FontSize="18" Foreground="White" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalAlignment="Left" Margin="66,0,0,0" />
                    <Button x:Name="TopBarSettingsButton" Content="Settings" Style="{StaticResource NoButtonStyle}" Height="26" FontWeight="SemiBold" FontSize="18" Foreground="White" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalAlignment="Left" Margin="80,0,0,0" />
                </UniformGrid>

                <!-- Right side: account name and logout aligned to the right column -->
                <StackPanel Grid.Column="1" Panel.ZIndex="101" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,39,0">
                    <Label x:Name="TopBarAccountName" Content="Name Here" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" FontWeight="Bold" FontSize="20" Foreground="White" Background="{x:Null}" BorderBrush="{x:Null}" Margin="0,0,10,0" Width="507" />
                    <Button x:Name="TopBarLogoutButton" Content="Logout" Style="{StaticResource NoButtonStyle}" Height="25" Width="90" Foreground="White" Background="{x:Null}" BorderBrush="{x:Null}" Click="TopBarLogoutButton_Click" FontSize="14" />
                </StackPanel>

                <!-- Accent separator line at the bottom of the TopBar, spans full width -->
                <Separator VerticalAlignment="Top" Panel.ZIndex="101" Height="1" Background="#FFE05953" Grid.ColumnSpan="3" Margin="0,59,0,0"/>
            </Grid>

            <!-- Placeholder for remaining content -->
            <Grid Grid.Row="1" Background="Transparent">
                <Canvas x:Name="RentalBrowserCanvas" HorizontalAlignment="Center" Height="990" VerticalAlignment="Center" Width="1894">
                    <Rectangle HorizontalAlignment="Left" Height="990" VerticalAlignment="Center" Width="368" Fill="#FF0F0F0F"/>
                    <Grid Height="926" Canvas.Left="24" Canvas.Top="32" Width="320">
                        <Label x:Name="SearchTitleLabel" Content="Search" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontWeight="Bold" FontSize="28" Foreground="White" Background="{x:Null}" BorderBrush="{x:Null}" Margin="0,10,0,858" />
                        <Label x:Name="EmailLabel" Content="City" FontSize="16" HorizontalContentAlignment="Left" Foreground="White" Margin="18,210,211,682" />
                        <TextBox x:Name="CityTextBox" TextWrapping="NoWrap" FontSize="16" Text="" VerticalContentAlignment="Center" BorderBrush="#FFE05953" Foreground="White" Background="{x:Null}" CaretBrush="White" Margin="18,244,18,643"/>
                        <Label x:Name="EmailLabel_Copy" Content="Make" FontSize="16" HorizontalContentAlignment="Left" Foreground="White" Margin="18,315,211,577" />
                        <TextBox x:Name="MakeTextBox" TextWrapping="NoWrap" FontSize="16" Text="" VerticalContentAlignment="Center" BorderBrush="#FFE05953" Foreground="White" Background="{x:Null}" CaretBrush="White" Margin="18,349,18,538"/>
                        <Label x:Name="EmailLabel_Copy1" Content="Model" FontSize="16" HorizontalContentAlignment="Left" Foreground="White" Margin="18,414,211,478" />
                        <TextBox x:Name="ModelTextBox" TextWrapping="NoWrap" FontSize="16" Text="" VerticalContentAlignment="Center" BorderBrush="#FFE05953" Foreground="White" Background="{x:Null}" CaretBrush="White" Margin="18,448,18,439"/>
                        <Label x:Name="EmailLabel_Copy2" Content="Year" FontSize="16" HorizontalContentAlignment="Left" Foreground="White" Margin="18,513,211,379" />
                        <TextBox x:Name="YearTextBox" PreviewTextInput="NumberValidationTextBox" MaxLength="5" TextWrapping="NoWrap" FontSize="16" Text="" VerticalContentAlignment="Center" BorderBrush="#FFE05953" Foreground="White" Background="{x:Null}" CaretBrush="White" Margin="18,547,18,340"/>
                        <ComboBox x:Name="ProvinceComboBox" IsReadOnly="True" Margin="18,138,18,749">
                            <ComboBoxItem Content=""/>
                            <ComboBoxItem Content="Alberta"/>
                            <ComboBoxItem Content="British Colombia"/>
                            <ComboBoxItem Content="Manitoba"/>
                            <ComboBoxItem Content="New Brunswick"/>
                            <ComboBoxItem Content="Newfoundland and Labrador"/>
                            <ComboBoxItem Content="Nova Scotia"/>
                            <ComboBoxItem Content="Northwest Territories"/>
                            <ComboBoxItem Content="Nunavut"/>
                            <ComboBoxItem Content="Ontario"/>
                            <ComboBoxItem Content="Prince Edward Island"/>
                            <ComboBoxItem Content="Quebec"/>
                            <ComboBoxItem Content="Saskatchewan"/>
                            <ComboBoxItem Content="Yukon"/>
                        </ComboBox>
                        <Label x:Name="ProvinceLabel" Content="Province" FontSize="14" HorizontalContentAlignment="Left" Foreground="White" Margin="18,103,184,788" />
                        <Button x:Name="SearchButton" Style="{StaticResource RoundedButton}" Content="Search" FontWeight="Bold" FontSize="14" Background="#FFE05953" BorderBrush="#FFB34944" Foreground="White" Margin="108,857,108,37" Click="SearchButton_Click"/>
                        <DatePicker x:Name="StartDate" Height="24" Canvas.Left="44" Canvas.Top="699" Width="284" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,655,0,0"/>
                        <Label Content="Start Date (Required)" FontSize="16" HorizontalContentAlignment="Left" Foreground="White" Margin="18,621,111,271" />
                        <DatePicker x:Name="EndDate" Height="24" Width="284" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,750,0,0"/>
                        <Label Content="End Date (Required)" FontSize="16" HorizontalContentAlignment="Left" Foreground="White" Margin="18,716,111,176" />
                    </Grid>
                    <ScrollViewer HorizontalAlignment="Center" Height="876" VerticalAlignment="Center" Width="1234" Canvas.Left="509" Canvas.Top="57" Foreground="White" Background="#FF0F0F0F">
                        <Border Padding="40">
                            <WrapPanel x:Name="RentalsListPanel" Orientation="Vertical" HorizontalAlignment="Center">
                                <Canvas Panel.ZIndex="10" Margin="0,20,0,0" Height="150" Width="1100" Background="#FF1A1A1A" Visibility="Collapsed">
                                    <Rectangle HorizontalAlignment="Center" Height="118" VerticalAlignment="Center" Width="219" Fill="White" Canvas.Top="16" Canvas.Left="16"/>
                                    <Label x:Name="CarNameLabel" Content="2000 Acura Integra GS" FontSize="38" FontWeight="SemiBold" HorizontalContentAlignment="Left" Foreground="White" Canvas.Left="258" Canvas.Top="16" HorizontalAlignment="Left" VerticalAlignment="Center" Width="580" Height="71" />
                                    <Button Panel.ZIndex="11" Visibility="Hidden" Height="150" Width="1100" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{x:Null}"></Button>
                                    <Label x:Name="CarPriceLabel" Content="$300" FontSize="38" FontWeight="SemiBold" HorizontalContentAlignment="Right" Foreground="White" Canvas.Left="906" Canvas.Top="16" Width="164" Height="71" HorizontalAlignment="Left" VerticalAlignment="Center" />
                                </Canvas>
                            </WrapPanel>
                        </Border>
                    </ScrollViewer>
                </Canvas>
                <!-- Your main content goes here -->
            </Grid>
        </Grid>
    </Viewbox>

</Window>
